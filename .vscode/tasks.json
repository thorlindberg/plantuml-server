{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run PlantUML Server",
            "type": "shell",
            "command": "java -jar plantuml.jar -picoweb",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
